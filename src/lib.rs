//! LogScope core library (Rust port).
//!
//! Phase 1: `models` + `core::parser`. Later phases add ingest, filter,
//! normalize, export, and pipeline.

pub mod core;
pub mod datetime;
pub mod gui;
pub mod models;
